use crate::question100::TreeNode;

// For each subtree, find the minimum value and maximum value of its descendants.

pub fn max_ancestor_diff(root: TreeNode) -> i32 {
    if root.left.is_none() && root.right.is_none() {
 
    }
    todo!()
}